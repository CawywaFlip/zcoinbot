import { Router } from "express";


const router = new Router();


// router.get('/orders/deps/:depIds', orderController.getOrderDeps);
// router.post('/orders', authMiddleware, await permissionMiddleware(perms.CAN_CREATE_ORDER), orderController.createOrder);
// router.put('/orders', authMiddleware, await permissionMiddleware(perms.CAN_EDIT_ORDER), orderController.editOrder);
// router.delete('/orders/revoke/:id', authMiddleware, await permissionMiddleware(perms.CAN_REVOKE_ORDER), orderController.revokeOrder);

export default router;


